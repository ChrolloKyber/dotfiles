---
- name: Change user settings
  hosts: localhost
  connection: local
  tasks:
    - name: Set zsh as default shell
      user:
        name: "{{ lookup('env', 'USER') }}"
        shell: /usr/bin/zsh
      become: true
